<Controls:MetroWindow x:Class="CashRegisterRepairs.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:CashRegisterRepairs.ViewModel"
        xmlns:uti ="clr-namespace:CashRegisterRepairs.Utilities"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:uti ="clr-namespace:CashRegisterRepairs.Utilities"
        xmlns:local="clr-namespace:CashRegisterRepairs.View"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        BorderThickness="0" 
        GlowBrush="Yellow"
        ResizeMode="CanMinimize"
        Name="LoginWindow"
        WindowTransitionsEnabled="False"
        WindowStartupLocation="CenterScreen"
        Title="Login" Height="300" Width="300">
    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid Background="PeachPuff">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Login" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="40" Width="250" Height="80"/>
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Username" FontWeight="UltraBold"/>
                <TextBox Text="{Binding Username}" Height="30" Width="180"/>
                <TextBlock Text="Password" FontWeight="UltraBold"/>
                <TextBox Text="{Binding Password}" Height="30" Width="180"/>
            </StackPanel>
        </Grid>
    </Grid>
</Controls:MetroWindow>
