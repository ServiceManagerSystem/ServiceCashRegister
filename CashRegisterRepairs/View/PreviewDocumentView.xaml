<Controls:MetroWindow x:Class="CashRegisterRepairs.View.PreviewDocumentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:vm="clr-namespace:CashRegisterRepairs.ViewModel"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CashRegisterRepairs.View"
        mc:Ignorable="d"
        Height="500"
        Width="700"
        GlowBrush="Yellow"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Title="PreviewDocumentView">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closed">
            <i:InvokeCommandAction Command="{Binding EnableSubviewDisplay}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Background="PeachPuff">
        <DockPanel Margin="10">
            <WrapPanel HorizontalAlignment="Center" DockPanel.Dock="Top" Margin="0,0,0,10">
                <Button Style="{StaticResource SaveOperationsButtonStyle}" Command="{Binding PrintDocumentCommand}" Content="PRINT"/>
            </WrapPanel>
            <TextBox Text="{Binding DocumentContent}"/>         
        </DockPanel>
    </Grid>
</Controls:MetroWindow>
